# 言智（yan-zhi）项目近期更新与风险分析

> 分析时间：2026-09-12 ｜ 分支：`dev0.1` ｜ HEAD：`9b1285d` feat: 新增代码工作台核心功能与git冲突解决能力
> 工作区：63 文件 modified（+4276/-1371）+ 9 项未跟踪，未提交

---

## 一、近期已提交更新（2026-09-05 ~ 2026-09-12）

近一周密集提交，围绕**代码工作台（/code IDE 模式）**、**Python 内置运行时**、**文件预览**、**运维 SFTP**、**皮肤扩充**、**插件分类**六大方向。

| 提交 | 内容 | 性质 |
|---|---|---|
| `9b1285d` | 代码工作台核心功能 + git 冲突解决能力 | 大特性 |
| `474a0be` | Excel 样式化预览（xlrd 脚本）、PDF 渲染、移除 Word 内嵌预览及相关依赖 | 体验优化 |
| `f5be164` | 代码模式体验修正：入口精简 + 图标 + 环境配置分 tab + 模式记忆 | 体验优化 |
| `a26f075` | CodeMirror 语言包纳入依赖完整性检查 | 维护 |
| `07b3908` | 代码模式：任务文件面板改造为 IDE 工作台（/code） | 大特性 |
| `ffa2fc5` | 目录树改为按层级懒加载（点开目录才加载其下一级） | 性能 |
| `0b8b5f1` | 内置 Python 运行时 + doyz 文档/网安工具/PDF 高保真预览（P1-P6） | 大特性 |
| `1d2b153` | 目录树滚动懒加载替代一次性截断 | 性能 |
| `6e25d73` | 新增 file_to_markdown 内置工具（办公文档→Markdown） | 新工具 |
| `c4937ee` | mammoth 与 SheetJS 的 Node 侧读取修复 | 修复 |
| `9c6c067` | 文件预览二进制乱码修复（PDF/Excel/Word） | 修复 |
| `63b3e1b` | 将 apps/desktop/build/installer.nsh 入库，修复 Windows 打包失败 | CI 修复 |
| `763fd7d` | 运维控制台 SFTP 文件管理 + 连接分组 + 插件 DB 沙箱 | 大特性 |
| `8c0d44f` | productName 言智→yan-zhi，安装目录规范化 + userData 迁移 | 构建 |
| `95fa2fa` | 清理残留 Electron 主进程，修复重跑叠窗口看到旧界面 | 修复 |
| `ba1c154` | 皮肤库扩充至 23 款 + surface 深度定制 | 大特性 |
| `03b66eb` | 内置智能体统一改名为助手 + dev0.1 功能更新 | 体验 |
| `b654f68` | 插件分类功能（功能/皮肤/操作/自定义） | 大特性 |
| `908dc05` | 皮肤系统 + 运维插件（ops-shell）+ 更多菜单分组接入 | 大特性 |

### 关键架构变化

1. **代码工作台（/code）**：从聊天模式独立出 IDE 风格代码开发模式，目录树懒加载、CodeMirror 多语言高亮、文件 tab、环境配置分 tab、任务面板改造为工作台
2. **Python 内置运行时**：改用 python-build-standalone 随包分发（离线可用），不再依赖宿主机 Python；附带 doyz 文档处理 / 网安工具 / PDF 预览脚本
3. **git 冲突解决**：新增三栏式冲突解决能力（对标 IDEA）
4. **运维 SFTP**：ops-shell 插件新增 SFTP 文件管理 + 连接分组 + 插件 DB 沙箱
5. **插件分类**：功能 / 皮肤 / 操作 / 自定义四类分组
6. **productName 规范化**：安装目录从「言智」改为「yan-zhi」，userData 迁移

---

## 二、未提交工作区改动（63 文件 modified，+4276/-1371）

改动覆盖面广，主要涉及：

| 区域 | 文件数 | 说明 |
|------|--------|------|
| git 插件 | ~10 | ChatGitPanel / git-explorer / git 路由 / git 服务 / git 工具（分支树、冲突解决、多仓库等） |
| 代码模式 UI | ~15 | AiDiffView / CodeCommandPalette / ChatFileTab / ChatContextSidebar / 各 code 组件 |
| 聊天 UI | ~10 | ChatInputArea / ChatMessageList / ChatPreviewPane / ChatSidebar / ScheduledTaskDialog |
| 后端路由/服务 | ~10 | llm-tasks / scheduled-tasks / plugins / git 路由 / ops-shell / db / index |
| 核心包 | ~5 | schema / plugin types / git 工具 / shared types |
| 打包配置 | 2 | electron-builder.full.yml / lite.yml |
| 其他 | ~11 | App.vue / BrowserPanel / chat.css / 各视图 |

### 建议

改动量大且涉及核心路径（git 插件、代码模式、后端路由），建议按主题拆分提交：
1. git 插件功能增强（分支树 / 冲突解决 / 多仓库）
2. 代码模式 UI 调整
3. 后端路由/服务改动
4. 打包配置调整
5. 其余 UI 微调

---

## 三、风险清单（按严重度）

### 🔴 高危
1. **工作区 63 文件未提交（+4276/-1371）**：改动量大且涉及核心路径（git 插件、代码模式、后端路由），丢失风险高。建议尽快分批提交。
2. **CI 不卡类型检查**：`build-desktop.yml` 只做打包，不跑 `typecheck` / `lint` / `test`，类型错误静默积累。**本次新增 `ci.yml` 后此风险消除**。

### 🟠 中危
3. **版本号不一致**：根 + desktop 为 0.1.1，server / web / mobile / ui / core / shared 仍为 0.1.0。**本次统一到 0.1.1 后此风险消除**。
4. **git 插件功能完整度与 IDEA/VSCode 差距**：用户反馈"跟 idea 这些开发工具里面的 git 功能差距还是很大"。已实现分支树 / 冲突解决 / 多仓库，但 pull/push/checkout 右键菜单、tag 子节点、未暂存/已暂存折叠等仍需逐项核对补齐。
5. **代码模式文件发黑/无高亮**：已知缺陷，模式切换、文件 tab 切换、点击浏览器后返回均会触发，需关掉再打开或再点一下。

### 🟡 低危
6. **LF/CRLF 行尾警告**：大量文件 "LF will be replaced by CRLF"，造成噪音 diff。
7. **根目录临时文件**：工作目录残留 `tmp_*`、`_*.log`、`nul`、`ELECTRON_RUN_AS_NODE=1` 等（多数已 gitignore），建议物理清理。

---

## 四、本次文档与 CI 更新（2026-09-12）

| 项目 | 内容 |
|------|------|
| README.md | 补充 10+ 新功能状态行；修正文档导航（移除已不存在的 `.trae/documents/` 引用）；更新打包说明（CI 只构建轻量版）；补充 Python 内置运行时说明；补全项目结构 |
| 项目近期更新与风险分析.md | 重写为 2026-09-12 快照（本文件） |
| .github/workflows/ci.yml | 新增：PR + push 触发，跑 typecheck / lint / test |
| 版本号 | server / web / mobile / ui / core / shared 统一到 0.1.1 |
| CHANGELOG.md | 新建，记录 0.1.0 → 0.1.1 变更 |
| apps/server/Dockerfile | 新增，支持 server 容器化部署 |

---

## 五、后续建议（优先级）

1. **分批提交工作区 63 文件改动**：按 git 插件 / 代码模式 / 后端 / 打包 / UI 拆分，避免单提交过大。
2. **git 插件功能逐项核对**：对照 IDEA/VSCode 补齐 pull/push/checkout 右键菜单、tag 子节点、未暂存/已暂存折叠、AI 填充提交信息、多仓库批量操作。
3. **代码模式文件发黑缺陷**：定位 CodeMirror / monaco 编辑器在模式切换 / tab 切换后的重新挂载时序问题。
4. **清理根目录临时文件**：`tmp_*`、`_*.log`、`nul`、`ELECTRON_RUN_AS_NODE=1` 等。
5. **CI 补全**：后续可考虑在 CI 中加入 Web / Mobile 构建、完整版桌面构建。
