# 言智（yan-zhi）项目近期更新与风险分析

> 分析时间：2026-08-30 ｜ 分支：`dev0.1` ｜ 状态：2 个提交领先 `origin/dev0.1` + 56 文件未提交改动
> 当前 HEAD：`f250eb4` chore: gitignore 补充（2026-08-29）

---

## 一、近期已提交更新（dev0.1 相对 origin 领先 2 提交）

| 提交 | 内容 | 性质 |
|---|---|---|
| `f250eb4` | gitignore 补充 coverage/tsbuildinfo/临时产物 | 清理 |
| `df3f6a4` | **本地模型改 Ollama 接入**；新增 license 授权系统（公钥+授权码）、scheduled-tasks 定时任务、search 搜索、agnes-platform 平台同步；修输入框深色配色 | 大特性 |

更早的提交线（已提交到 dev0.1）还包含：
- **IA 信息架构重构**：5 项导航 + 对话重皮肤 + 设置抽屉 + 默认暗色主题（`fcdae10`/`7a80c44`）
- **知识库实体图谱 + 多层记忆 + 工作目录 + 浏览器桥接**（`be910be`）
- **桌面端从 Tauri 迁移到 Electron**，内置浏览器改用原生 BrowserView 注入滚动条/缩放（`82fe386`/`e4e4496`/`505c900` 清 Tauri 废弃代码）
- **Anthropic 协议支持**、平台配置卡片（`9106fb0`）

**关键架构变化**：`df3f6a4` 整体删除了 `apps/server/src/local-model/`（engine 212 行、router 397 行、tool-call-adapter 150 行、embedding-engine 128 行），本地推理改由 **Ollama（:11434）** 提供。

---

## 二、未提交工作区改动（最大风险源）

`git diff --stat`：**56 文件，+3391 / -1036**。最重改动：

| 文件 | 改动 | 说明 |
|---|---|---|
| `apps/desktop/main.cjs` | +251 | BrowserView 桥接 IPC（叠加 df3f6a4 已改 567 行，处于频繁变动）|
| `apps/server/src/routes/browser.ts` | +307 | 浏览器路由大幅扩展 |
| `packages/core/src/tool/builtin/browser/index.ts` | +223 | 新增浏览器工具 |
| `packages/ui/src/views/ChatHub.vue` | +436 | 对话主区重构 |
| `packages/ui/src/views/Connections.vue` | +546 | 连接页重构 |
| `packages/ui/src/components/BrowserPanel.vue` | +178 | 内置浏览器面板 |
| `packages/core/src/db/schema.ts` | +193 | 新增 plugin/plugin_storage 表 + skills 种子 |
| `apps/server/src/db.ts` | +123 | DB 层扩展 |
| `pnpm-lock.yaml` | 440 增 / 671 删 | **整个 lockfile 被重写** |

新增依赖（未提交）：`packages/ui` 加 CodeMirror 全家桶 + qrcode + jsqr（**代码编辑器 + 二维码**）；`apps/server` 加 adm-zip + simple-git（**压缩包 + git 操作**）。

---

## 三、类型检查现状（已失败）

`typecheck_output.txt`（UTF-16，被误读为 0，实际 **43 个 `error TS`**），由 `@yan-zhi/desktop` 的 `vue-tsc --noEmit` 产生：

- **32 个错误位于已提交代码**（api-tools / sandbox / get-api-tools / tools.ts / FilePreview / SolarSystem / EarthMap / AgentCanvas / Models / Agents）—— 说明类型债务在提交前已积累。
- **11 个由本次未提交改动新引入**：`SideNav.vue`(3)、`Chat.vue`(7)、`stores/chat.ts`(1) —— 正是 IA 重构涉及的文件。

错误分类：
- `TS2339` ×20：属性不存在（如 SideNav `key`、tools.ts `data`、Chat.vue）—— **真实的类型不匹配，可能对应运行时空值访问**。
- `TS2580` ×12：`require` 未定义 —— 出现在 `packages/core`（ESM 包）的 api-tools / get-api-tools / sandbox。**ESM 下 `require` 运行时会真正抛错**，属潜在运行时崩溃，不只是类型问题。
- `TS2307` ×1：`node:vm` 模块找不到（sandbox.ts）—— core 包缺 `@types/node`。
- `TS7016` ×2：markdown-it 等缺类型声明。

> 注意：CI 的 `electron:build` 走 Vite/esbuild，**不执行类型检查**，所以这些错误不会阻断构建，只会静默累积、运行时才暴露。

---

## 四、风险清单（按严重度）

### 🔴 高危
1. **工作区大批量未提交 + 2 提交未推送**：56 文件改动（+3391/-1036）全部在工作区，dev0.1 还领先 origin 2 提交。机器/ workspace 一旦丢失或误操作，全部工作归零；且难以 code review。
2. **typecheck 全面失败（43 错误）且 CI 不卡类型**：类型错误静默积累。`packages/core` 在 ESM 中用 `require` ×12，构建产物运行时可能直接崩。
3. **本地模型引擎删除改 Ollama，但桌面端 prepare 脚本仍安装 `node-llama-cpp`**（prepare-server-runtime.cjs:111/165、download-llama-server.cjs）—— **孤儿依赖**：CI 仍需下载对应平台包（mac-arm64-metal 等），增加安装脆弱性且无运行时收益；同时用户必须自备 Ollama(:11434)，否则本地模型功能不可用。
4. **内置浏览器（BrowserView 桥接）多处大改**：main.cjs / server browser 路由 / core browser 工具 / UI BrowserPanel 全动，正好触及此前已知「/browser 路由白屏」的敏感区，回归风险高。

### 🟠 中危
5. **DB 迁移安全性**：schema.ts 新增 `plugin` / `plugin_storage` 表（CREATE TABLE IF NOT EXISTS，新库安全）+ 大量 skills 种子。需确认老库迁移与种子 upsert 的幂等性；`plugin` 表疑似无对应运行时（半成品 schema）。
6. **依赖大改 + lockfile 整体重写**（671 删/440 增）：CodeMirror / QR / adm-zip / simple-git 新引入。版本号抽样未见漂移，但整体重锁可能改变传递依赖，需保证各机 `pnpm install` 一致性（尤其 esbuild / better-sqlite3 / playwright 原生包）。
7. **含 license 授权系统（公钥 + 鉴权改动）未推送**：敏感代码仅在本机，协作/备份风险；授权逻辑正确性未验证。
8. **CI 仍用 Node 22**（非已规划的 24）；prepare 脚本平台映射虽已修正（`win32-x64`/`darwin-x64`/`darwin-arm64→mac-arm64-metal`/`linux-x64`），但仍在装 node-llama-cpp（见 3）。

### 🟡 低危
9. **LF/CRLF 行尾警告**：大量文件 "LF will be replaced by CRLF"，造成噪音 diff 与潜在合并冲突。
10. **Anthropic 支持**：提交历史显示已补（`9106fb0`），此前的「LlmClient 仅 OpenAI」风险项**看起来已解决**，建议核对 `@yan-zhi/core` 实际是否双协议可用。

---

## 五、建议（优先级）

1. **立即提交/推送当前工作**：先把 56 文件按模块拆成若干提交（至少 browser 桥接、IA 重构、DB/插件、依赖各一），并 `git push` 到 dev0.1，消除丢失风险。
2. **跑一次干净 typecheck 并清零**：优先修 `packages/core` 的 `require`/`node:vm`（加 `@types/node` 或改用 `import`）—— 这是唯一会运行时崩的硬伤；其余 TS2339 逐一定位。
3. **清理孤儿 node-llama-cpp**：若运行时已完全走 Ollama，从 prepare-server-runtime.cjs / download-llama-server.cjs 移除 node-llama-cpp 安装，避免 CI 装无用原生包。
4. **验证 Ollama 兜底 UX**：本地模型页在 Ollama 未启动时应有清晰引导，而非静默失败。
5. **浏览器路由回归测试**：重点验证桌面端 /browser 路由是否仍白屏（历史已知问题）。
6. **DB 迁移演练**：在已有库上跑 `initSchema`，确认 plugin 表与 skills 种子幂等、无破坏。
7. **CI 升级 Node 24**（按既定规划），并考虑在 CI 增加 `vue-tsc --noEmit` 关卡防止类型债务继续累积。

---

### 附：43 个类型错误分布
```
11  packages/core/src/tool/builtin/api-tools/index.ts   (require)
 9  packages/ui/src/stores/tools.ts                      (data 联合类型)
 7  packages/ui/src/views/Chat.vue
 3  packages/ui/src/components/home/SolarSystem.vue     (import.meta.env / BufferGeometry)
 3  packages/ui/src/components/SideNav.vue               (key)
 2  packages/ui/src/views/AgentCanvas.vue
 2  packages/ui/src/views/Models.vue
 1  packages/core/src/tool/builtin/get-api-tools.ts      (require)
 1  packages/core/src/tool/sandbox.ts                    (node:vm)
 1  packages/ui/src/components/FilePreview.vue           (markdown-it)
 1  packages/ui/src/components/home/EarthMap.vue         (import.meta.env)
 1  packages/ui/src/stores/chat.ts                       (messages)
 1  packages/ui/src/views/Agents.vue
```
