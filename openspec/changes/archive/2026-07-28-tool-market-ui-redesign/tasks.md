## 1. 页面整体结构替换

- [x] 1.1 移除 `el-tabs` 和 `tab` ref，替换为两个 `<section>` 容器（MCP 服务 + 工具商城）
- [x] 1.2 添加 `activeMarketId` ref（`string | null`）追踪当前展开的商城子视图
- [x] 1.3 实现主视图 ↔ 子视图切换（`activeMarketId === null` 为主视图，否则为子视图）

## 2. MCP 服务概览区

- [x] 2.1 实现紧凑型 MCP 卡片列表（仅展示名称、连接状态灯、工具数量），复用 `mcpStore`
- [x] 2.2 添加"查看全部 MCP 服务 →"链接，点击跳转 `/mcp`
- [x] 2.3 处理空状态：无 MCP 服务时显示提示 + "前往添加"按钮跳转 `/mcp`

## 3. 商城卡片网格（主视图）

- [x] 3.1 实现"本地商城"卡片（始终在网格首位），包含图标、名称、内置/自定义工具数量摘要
- [x] 3.2 实现远程商城源卡片（从 `toolsStore.remoteSources` 读取），每个源一张卡片
- [x] 3.3 添加"新增远程商城"按钮（工具栏），点击打开新增远程源 Dialog
- [x] 3.4 实现新增远程源 Dialog（复用现有 `showSourceForm` 逻辑和模板）
- [x] 3.5 点击商城卡片时设置 `activeMarketId` 切换到子视图

## 4. 本地商城子视图

- [x] 4.1 实现面包屑/返回按钮导航回主视图
- [x] 4.2 实现"内置工具"区域：只读卡片列表，展示工具名、描述、"内置"标签
- [x] 4.3 实现"自定义工具"区域：可编辑卡片列表，含启用开关、编辑、删除按钮
- [x] 4.4 在自定义工具区域添加"新增工具"按钮（仅本地商城可见）
- [x] 4.5 复用现有新增/编辑工具 Dialog 的逻辑和模板

## 5. 远程商城子视图

- [x] 5.1 实现远程商城详情头部（商城名称、URL、操作按钮：测试连接、删除源）
- [x] 5.2 从远程源 fetch 工具列表并展示为卡片（工具名、描述、"安装到本地"按钮）
- [x] 5.3 实现"安装到本地"功能（复用 `toolsStore.installFromMarket`）

## 6. 路由清理

- [x] 6.1 移除 `router/index.ts` 中重复的 `/tools` 路由定义

## 7. 样式优化

- [x] 7.1 商城卡片 UI：使用玻璃态卡片样式，添加商城专属图标/配色区分本地 vs 远程
- [x] 7.2 子视图过渡动画：使用 `<Transition>` 包裹主视图 ↔ 子视图切换
- [x] 7.3 MCP 概览区紧凑样式：网格列宽适配小卡片（minmax(240px, 1fr)）
- [x] 7.4 整体间距、字号、颜色变量对齐设计系统

## 8. 验证

- [ ] 8.1 验证本地商城子视图可正常查看内置工具和自定义工具 ⚡
- [ ] 8.2 验证只有本地商城才显示"新增工具"按钮 ⚡
- [ ] 8.3 验证新增远程商城源流程正常（添加 → 卡片显示 → 浏览 → 安装工具）⚡
- [ ] 8.4 验证面包屑返回主视图正常 ⚡
- [ ] 8.5 验证 MCP 概览跳转到 `/mcp` 正常 ⚡
- [ ] 8.6 端到端：从工具管理页面完成"查看本地工具 → 新增自定义工具 → 添加远程商城 → 安装远程工具到本地"全流程 ⚡

> ⚡ = 需要启动应用运行时验证
