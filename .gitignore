node_modules/
dist/
.DS_Store
*.local
.env
.env.*
!.env.example
.codegraph/

# Build outputs
*.js
*.js.map
*.d.ts
*.d.ts.map
!*.config.js
!vitest.config.ts
!eslint.config.*


# Tauri
apps/desktop/src-tauri/target/
apps/desktop/src-tauri/Cargo.lock

# Electron 构建产物
dist-release/
dist-release2/
dist-download/
apps/desktop/build/*
!apps/desktop/build/installer.nsh
apps/desktop/runtime/
apps/desktop/release-lite/
apps/desktop/release/

# 内置 Python 运行时（构建时由 scripts/build-python-runtime.mjs 生成，体积大，不入库）
# prod 运行时从 resources/ 拷贝进成品；dev 模式回退系统 python，无需此目录
apps/desktop/resources/python/
apps/desktop/resources/python-tools/
apps/desktop/resources/_pbs_*/

# Electron/本地调试产物
apps/server/bsqlite-*.txt
apps/server/smoke-*.txt
apps/server/electron-*.mjs

# Capacitor
apps/mobile/ios/
apps/mobile/android/
*.apk
*.ipa

# IDE
.vscode/
.idea/
.trae/
*.swp

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# 数据库
*.db
*.db.bak-*
*.db-journal
*.db-shm
*.db-wal
*.sqlite

# Corepack
.corepack-cache/

# Claude local settings
.claude/settings.local.json

tmp/
.workbuddy/

# 环境/临时文件（不应入库）
_rustup-init.exe
rustup-init.exe
_diag.txt
*.timestamp-*.mjs
apps/server/workspace/

# 本地模型权重（体积较大，不在 Git 中提交）
apps/server/models/
*.gguf
*.gguf.part

# llama-server 预编译二进制（构建时从 llama.cpp release 下载，不入库）
apps/server/bin/

# 授权码签发侧（私钥 + 签发工具）：只在签发者本机存在，绝不入库。
# 应用侧只需要公钥（apps/server/src/license-public-key.pem，可入库）与验签逻辑。
# 签发工具本身不含私钥，但它与本目录的私钥同处一地 —— 入库会随 clone 分发到
# 任何构建机，把「私钥和工具放一起」的事故面放大，故一并忽略。
scripts/license-private-key.pem
scripts/gen-license.mjs
scripts/license-manager.html

# 测试覆盖率 / TS 构建缓存 / 临时产物
coverage/
*.tsbuildinfo
typecheck_output.txt
_check_db.py
*分析报告.md
*完整性检查.md

# playwright-cli 浏览器脚本临时产物（日志 / 快照 / 截图）
.playwright-cli/
openspec/changes/data-ontology-agent/shots/

# 一次性修复 / 诊断 / 检查脚本（用后即弃，不入库）
scripts/patch-*.py
scripts/patch-*.cjs
scripts/fix-*.py
scripts/fix-*.cjs
scripts/diagnose-*.py
scripts/diagnose-*.cjs
scripts/check-*.py
apps/server/diagnose-cdp.cjs

# 根目录临时调试产物
tmp_*
tmp_smoke.out
tmp_server.err
nul
*.bak

# 移动端内置 Node.js 运行时（构建时下载，不入库）
apps/mobile/nodejs/

# 服务端临时冒烟数据
apps/server/tmp_*

# CodeBuddy 工作区数据（不入库）
.codebuddy/

# 图标重建临时输出与历史打包变体
dist-release-block/
apps/desktop/release-compress/
apps/desktop/release-final/
apps/desktop/release-fixed/
apps/desktop/release-full/
apps/desktop/release-lite-old-1.0/
apps/desktop/release-lite-v2-prev/
apps/desktop/release-lite-v3-prev/
apps/desktop/release-lite-v4-prev/
apps/desktop/release2/
apps/desktop/release3/
apps/desktop/release4/

# electron-build.cjs 重定位输出时生成的临时配置
apps/desktop/electron-builder.*.relocated.yml

# 重定位构建输出与临时垃圾
yanzhi-dist-release/
_tmp_*

# 运行时数据（浏览器历史等，不入库）
workspace/
# 临时 demo 页
list-fix-demo.html

# 运行时按需下载的 ffmpeg 二进制（media_install_ffmpeg 落盘位置，约 196MB，不入库）
apps/server/ffmpeg/

# 运行时按需下载的语音引擎与模型（sherpa-onnx 原生库约 22MB + 语音包约 31MB，不入库）
apps/server/sherpa/
apps/server/tts-models/

# TTS 试听与合成产物（运行时生成，不入库）
apps/server/generated-audios/
